# /files folder

This folder is no longer used by the site.

The site no longer serves downloadable files from this location. Access is
handled through encrypted payloads in the `data/` folder, decrypted
client-side using a per-page SecretKey.

**Credentials are never stored in this repository.** All SecretKeys are
distributed out-of-band to authorized users.
